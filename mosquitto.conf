# SignGuard AI — Mosquitto broker config
# Hackathon settings: open on the LAN so the Pi and ESP32 can connect.
# Do NOT use allow_anonymous in production.

listener 1883 0.0.0.0
allow_anonymous true

persistence false
log_dest stdout
